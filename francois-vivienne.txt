

User abrufen:
await getUserData();
console.log(contacts);

----

neuen User erstellen (für sign up):

let user = {};
user.name = 'blablabla';
user.title = 'bliblibli';
user.password = 'blublublu';
user.loggedIn = true;
await createContact(user);
signUp();

---

User einloggen:
die globale Variable 'loggedInUserId' anpassen und signIn aufrufen

loggedInUserId = user.Id
signIn();

---

User ausloggen:
einfach 'signOut()' aufrufen

---

